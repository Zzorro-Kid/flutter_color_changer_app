name: flutter_color_changer_app
description: "A Flutter app that changes background color on tap."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  get_it: ^9.2.0
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  solid_lints: ^0.3.3

flutter:
  uses-material-design: true
